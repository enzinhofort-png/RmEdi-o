# ============================================================
# ClipStudio — Variáveis de Ambiente
# Renomeie este arquivo para .env e preencha os valores
# NUNCA commite o .env no git (já está no .gitignore)
# ============================================================

# ── Supabase ────────────────────────────────────────────────
# Encontre em: supabase.com → seu projeto → Settings → API

VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ── Opcional: OAuth providers ────────────────────────────────
# Configure em: Supabase → Authentication → Providers
# Google: console.cloud.google.com → APIs → Credentials
# GitHub: github.com → Settings → Developer settings → OAuth Apps

# Não é necessário aqui — configure direto no painel Supabase
